description: "Context iteration with template rendering across adapters"

variants:
  builtin:
    description: "Built-in interpolator with context iteration"
    implementations: [python, typescript]
  mako:
    description: "Mako adapter with context iteration"
    implementations: [python]
  jinja2:
    description: "Jinja2 adapter with context iteration"
    implementations: [python]
  eta:
    description: "Eta adapter with context iteration"
    implementations: [typescript]
  nunjucks:
    description: "Nunjucks adapter with context iteration"
    implementations: [typescript]
